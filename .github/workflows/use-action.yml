name: Test my action

on: [push]

jobs:
  test-my-action:
    runs-on: ubuntu-latest
    permissions:
      # Give the default GITHUB_TOKEN write permission to commit and push the
      # added or changed files to the repository.
      contents: write
    steps:
      - name: Update README with my action
        uses: testuserrrrrrr/my-action-demo2@2e657c755154a12d45f20a64827b125cbc9aa80e
        with:
          ignore: "AAAAAAAAAAAAAAA"

